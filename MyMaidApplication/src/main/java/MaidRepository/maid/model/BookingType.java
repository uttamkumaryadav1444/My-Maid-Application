package MaidRepository.maid.model;

public enum BookingType {
    HOURLY,     // Per hour booking
    DAILY,      // Per day booking
    WEEKLY,     // Per week booking
    MONTHLY     // Per month booking
}